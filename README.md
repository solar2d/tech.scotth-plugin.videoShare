Sources for the plugin `plugin.videoShare`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.videoShare" = {
            publisherId = "tech.scotth",
        },
    },
}
```
